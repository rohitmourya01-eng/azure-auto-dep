apiVersion: v1
kind: Service
metadata:
  name: auto-deployment
  namespace: namespace-workflow-1786001825842
  labels:
    app.kubernetes.io/name: auto-deployment
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: auto-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80